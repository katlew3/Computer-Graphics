#version 330 core

in vec4 vs_vertexColor;

// Ouput data
out vec4 color;

// Values that stay constant for the whole mesh.
uniform float PickingColor;
uniform float PickingColorG;

void main(){

	color = vec4(PickingColor, PickingColorG, 0.0, 1.0);

}